# image2ascii
For printing images and videos as ascii art

## USAGE
Use `python commands/convert_image.py --help` for instructions on images.
Use `python commands/play_video.py --help` for instructions on videos.